{% extends 'base1.html.twig' %}

{% block body %}



    <div class="container-fluid">

        <div class="row">

            <div class="col-12">
                <div class="card-box">

                    <h4 class="m-t-0 header-title">Les Ordonnances</h4>
                    <p class="text-muted font-14 m-b-20">


                    <table class="table">
        <thead>
            <tr>
                <th>Id</th>

                <th>Date</th>
                <th> Description </th>
                <th> Traitement </th>
                <th> actions </th>
            </tr>
        </thead>
        <tbody>
        {% for ordonnance in ordonnances %}
            <tr>

                <td>{{ ordonnance.id }}</td>
                <td>{{ ordonnance.date ? ordonnance.date|date('Y-m-d') : '' }}</td>
                <td>{{ ordonnance.description }}</td>
                <td>{{ ordonnance.traitement }}</td>
                <td>

                    <a href="{{ path('app_ordonnance_edit', {'id': ordonnance.id}) }}"> <button class="btn btn-outline-primary" > update</button></a>


                    <a href="{{ path('app_ordonnance_show', {'id': ordonnance.id}) }}"><button class="btn btn-outline-primary" > show</button></a>
                    <a href="{{ path('app_ordonnance_delete', {'id': ordonnance.id}) }}"><button class="btn btn-outline-primary" > delete</button></a>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
            </div>
        </div>
    </div>
</div>

    <a href="{{ path('app_ordonnance_new') }}"><button class="btn btn-outline-primary" >Create new</button> </a>
{% endblock %}
