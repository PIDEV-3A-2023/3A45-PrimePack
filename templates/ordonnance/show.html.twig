{% extends 'base1.html.twig' %}

{% block body %}
    <h1>Votre Ordonnance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordonnance.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ ordonnance.date ? ordonnance.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ordonnance.description }}</td>
            </tr>
            <tr>
                <th>Traitement</th>
                <td>{{ ordonnance.traitement }}</td>
            </tr>

        </tbody>
    </table>


    <a href="{{ path('app_ordonnance_edit', {'id': ordonnance.id}) }}"><button class="btn btn-outline-primary" > edit</button> </a>

    <a href="{{ path('show_pdf', {'id': ordonnance.id}) }}"><button class="btn btn-outline-primary" > Imprimer</button></a>


    {{ include('ordonnance/_delete_form.html.twig') }}

    <a href="{{ path('app_ordonnance_index') }}">back to list</a>

{% endblock %}
